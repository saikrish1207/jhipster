version: '2'
services:
  ordercmd-mongodb:
    image: mongo:4.0.12
    ports:
      - '27017:27017'
    command: mongos --configdb ordercmd-mongodb-config
  ordercmd-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  ordercmd-mongodb-config:
    image: mongo:4.0.12
    container_name: ordercmd-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
